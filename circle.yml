machine:
  node:
    version: 0.10.40
  post:
    - curl https://install.meteor.com | /bin/sh
    - npm install -g phantomjs
    - npm install -g spacejam
test:
  override:
    - spacejam test-packages ./ --use-system-phantomjs --mocha --release 1.2.1
